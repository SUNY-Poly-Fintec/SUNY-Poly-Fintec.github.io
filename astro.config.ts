import { defineConfig } from 'astro/config';


export default defineConfig({
site: 'https://SUNY-Poly-Fintec.github.io/fintech-club-site', // or https://YOUR-ORG.github.io
//base: '/fintech-club-site/', // REMOVE this line if using the org root repo
});
